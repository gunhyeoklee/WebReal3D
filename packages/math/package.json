{
  "name": "@web-real/math",
  "type": "module",
  "main": "dist/web-real-math.cjs",
  "module": "dist/web-real-math.mjs",
  "types": "dist/types/index.d.ts",
  "files": ["dist"],
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/web-real-math.mjs",
      "require": "./dist/web-real-math.cjs"
    }
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json && vite build --config vite.config.ts",
    "test": "vitest run --config vitest.config.ts"
  }
}
